@model  Model.AlumnoCurso
@{
    List<Model.Curso> cursos = ViewBag.Cursos;
}

@using (Html.BeginForm("Guardar", "Home", FormMethod.Post, new { id = "frm-alumno" }))
{
    @Html.HiddenFor(x => x.Alumno_id)
    <div class="form-group">
        <label>Seleccione un curso</label>
        <select class="form-control" name="Curso_id">
            @foreach (var c in cursos)
            {
                <option value="@c.id">@c.Nombre</option>
            }
        </select>
    </div>
    <div class="form-group">
        @Html.LabelFor(x => x.Nota)
        @Html.TextBoxFor(x => x.Nota, new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.Nota, null, new { @class = "text-danger" })
    </div>

    <div class="text-right">
        <button data-ajax="true" data-confirm="¿Esta seguro de asignar este curso al alumno?" type="submit" class="btn-sm btn btn-success">Guardar</button>
    </div>
}

<!--CURSOS ELEGIDOS-->
<ul class="list-group"></ul>